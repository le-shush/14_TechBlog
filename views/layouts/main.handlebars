<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Meta information for character set and viewport -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <!-- Page title -->
    <title>Technology Blog</title>
    
    <!-- Stylesheets for the page -->
    <link rel="stylesheet" href="/css/jass.css">
    <link href="/css/style.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
</head>
<body>
    <!-- Header section with navigation -->
    <header class="pb-2 bg-dark" data-bs-theme="dark">
        <nav class="navbar navbar-expand-lg text-center">
            <div class="container-fluid">
                <!-- Navigation links -->
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" href="/">Main Page</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/home">Control Panel</a>
                    </li>
                    <!-- Conditional rendering based on login status -->
                    {{#if logged_in}}
                    <li class="nav-item">
                        <button type="button" class="btn btn-link bg-dark" id="logout">Sign Out</button>
                    </li> 
                    {{else}}
                    <li class="nav-item">
                        <a class="nav-link" href="/login" id="login">Sign In</a>
                    </li>
                    {{/if}}
                </ul>    
            </div>
        </nav>
        <!-- Blog title -->
        <div class="container text-center position-relative">
            <h1 class="fluid main-title bg-white pb-1">The Technology Blog</h1>
        </div>
    </header>
    
    <!-- Main content area -->
    <div class="bg-warning-subtle pt-2 main-content">
        <main>
            <!-- Dynamic content will be injected here -->
            {{{ body }}}
        </main>
    </div>
    
    <!-- Bootstrap and Font Awesome scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    <script src="https://kit.fontawesome.com/3f7bdb791a.js" crossorigin="anonymous"></script>
    
    <!-- Conditional script based on login status -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>
</html>
